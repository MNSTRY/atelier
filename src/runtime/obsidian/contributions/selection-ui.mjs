import { createSelectionContribution } from '../../../projection/obsidian/selection-ui/contribution.mjs'

// The selection, conflict and apply-policy operations: `atelier obsidian selection`, `conflicts` and `apply-policy`.
export default createSelectionContribution()
