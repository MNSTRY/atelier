import { createSourceApplyContribution } from '../../../projection/obsidian/edits/contribution.mjs'

// The source apply operation: what the engine dispatches to in automatic mode, and `atelier obsidian apply`.
export default createSourceApplyContribution()
